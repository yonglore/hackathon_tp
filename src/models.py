"""Доменные структуры данных, общие для всего проекта.

Владелец: Вадим.
Это «контракт», на который опираются все остальные модули, поэтому файл
делается ПЕРВЫМ. Точный состав полей команда фиксирует на старте (день 1).

TODO: реализовать. Здесь только каркас интерфейса, логику/поля пишет команда.
"""

from dataclasses import dataclass


@dataclass
class Email:
    """Одно письмо, прочитанное из inbox.

    TODO: определить набор полей. Ориентировочно может понадобиться:
        - path        : путь к исходному файлу
        - sender       : отправитель (если удалось распарсить)
        - subject      : тема
        - body         : текст письма
        - raw          : сырое содержимое / служебные данные
        - is_readable   : удалось ли корректно прочитать
    Точный список — на усмотрение команды.
    """
    # TODO: добавить поля
    pass


# TODO (по желанию): класс/enum Category для перечня категорий,
# либо это можно держать в config.py. Решить на старте.
