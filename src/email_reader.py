"""Чтение и парсинг писем из папки inbox.

Отвечает за критерий 3 (файловая система и исключения): разные форматы,
неожиданные кодировки, нечитаемые/повреждённые файлы(приложение не должно ломаться)

TODO: реализовать
"""

from pathlib import Path

from .models import Email


def read_all(inbox_dir: Path) -> list[Email]:
    """Прочитать все письма из inbox_dir и вернуть список Emails.

    TODO:
        обойти все файлы в папке;
        на каждый вызвать read_one;
        НЕ падать на проблемном файле (логировать и идти дальше).
    """
    raise NotImplementedError


def read_one(path: Path) -> Email:
    """Прочитать одно письмо.

    TODO:
        - открыть файл, обработать разные форматы/кодировки;
        - корректно отработать пустой/повреждённый/неизвестный файл
          (вернуть Email с пометкой «нечитаемое» или бросить понятное исключение —
          решить на старте и согласовать с classifier/main).
    """
    raise NotImplementedError
